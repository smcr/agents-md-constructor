# Example only. Create the real secret in the cluster, do not commit credentials.
# kubectl create secret generic agents-md-creator \
#   --from-literal=DATABASE_URL='postgresql://user:pass@postgres:5432/agents'
apiVersion: v1
kind: Secret
metadata:
  name: agents-md-creator
type: Opaque
stringData:
  DATABASE_URL: postgresql://agents:agents@postgres:5432/agents
